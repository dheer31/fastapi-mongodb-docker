services:
  db:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      MONGO_URL: mongodb://db:27017
      MONGO_DATABASE: fastapi_db
    depends_on:
      - db

volumes:
  mongo_data:
